version: '3'

services:

  web:
    image: fleek/jekyll
    command: jekyll serve --drafts --watch --force_polling --livereload
    ports:
      - 4000:4000
      - 35729:35729
    volumes:
      - .:/srv/jekyll